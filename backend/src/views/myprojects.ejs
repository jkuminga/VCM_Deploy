<!DOCTYPE html>
<html lang="ko">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>내 프로젝트 목록</title>
    <style>
      :root {
        font-family: 'Segoe UI', -apple-system, BlinkMacSystemFont, sans-serif;
        color: #061b16;
        background-color: #f7fbf9;
      }

      * {
        box-sizing: border-box;
      }

      body {
        margin: 0;
        min-height: 100vh;
        display: flex;
        background: #f4f9f4;
        color: #061b16;
      }

      .sidebar {
        width: 24%;
        min-width: 240px;
        padding: 32px 24px;
        background: linear-gradient(180deg, #2b5876, #4e4376);
        color: #f4fff3;
        display: flex;
        flex-direction: column;
        gap: 24px;
      }

      .profile-card {
        background: rgba(244, 255, 243, 0.08);
        border-radius: 16px;
        padding: 20px;
        text-align: center;
      }

      .profile-card h2 {
        margin: 0;
        font-size: 1.4rem;
        letter-spacing: 0.1em;
      }

      .profile-card p {
        margin: 6px 0 0;
        opacity: 0.8;
      }

      .sidebar nav a {
        display: block;
        padding: 10px 16px;
        margin-bottom: 8px;
        border-radius: 12px;
        color: inherit;
        text-decoration: none;
        transition: background 0.2s ease;
      }

      .sidebar nav a:hover {
        background: rgba(244, 255, 243, 0.15);
      }

      .back-link {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        color: #f4fff3;
        text-decoration: none;
        font-weight: 600;
      }

      .content {
        flex: 1;
        padding: 32px 40px;
      }

      .project-grid {
        display: grid;
        grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
        gap: 20px;
      }

      .project-widget {
        background: #fff;
        border-radius: 24px;
        padding: 28px;
        box-shadow: 0 20px 45px rgba(0, 0, 0, 0.08);
        border: 1px solid rgba(6, 27, 22, 0.05);
      }

      .project-widget h2 {
        margin: 0 0 6px;
        font-size: 1.2rem;
      }

      .project-widget .project-id {
        font-size: 0.9rem;
        color: #0f5132;
        margin-bottom: 12px;
        letter-spacing: 0.1em;
      }

      .project-stats {
        display: grid;
        grid-template-columns: repeat(2, minmax(0, 1fr));
        gap: 12px;
        margin-top: 18px;
      }

      .stat {
        background: #f4f9f4;
        border-radius: 16px;
        padding: 14px;
      }

      .stat-label {
        font-size: 0.85rem;
        opacity: 0.7;
        margin-bottom: 6px;
      }

      .stat-value {
        font-weight: 600;
        font-size: 1.1rem;
      }

      .empty-message {
        text-align: center;
        font-size: 1rem;
        color: rgba(6, 27, 22, 0.6);
        padding: 40px 0;
      }

      @media (max-width: 860px) {
        body {
          flex-direction: column;
        }
        .content {
          padding: 24px;
        }
      }
    </style>
  </head>
  <body>
    <aside class="sidebar">
      <a class="back-link" href="/projects">
        ← 대시보드로 돌아가기
      </a>
      <div class="profile-card">
        <h2>VCM</h2>
        <p>My Page</p>
      </div>
      <nav>
        <a href="/user/mypage">회원 정보 확인</a>
        <a href="/user/projects">내가 등록한 프로젝트 목록</a>
      </nav>
    </aside>

    <main class="content">
      <h1>내가 등록한 프로젝트</h1>
      <section class="project-grid">
        <% if (Array.isArray(data) && data.length) { %>
        <% data.forEach((project)=> { %>
        <article class="project-widget">
          <div class="project-id"><%= project.project_id %></div>
          <h2><%= project.project_name %></h2>
          <p><strong>Registry:</strong> <%= project.registry %></p>
          <p><strong>Vintage:</strong> <%= project.vintage || 'N/A' %></p>
          <div class="project-stats">
            <div class="stat">
              <div class="stat-label">Total Issued</div>
              <div class="stat-value"><%= project.total_issued?.toLocaleString('en-US') || 0 %></div>
            </div>
            <div class="stat">
              <div class="stat-label">Total Retired</div>
              <div class="stat-value"><%= project.total_retired?.toLocaleString('en-US') || 0 %></div>
            </div>
          </div>
        </article>
        <% }) %>
        <% } else { %>
        <div class="empty-message">등록된 프로젝트가 없습니다.</div>
        <% } %>
      </section>
    </main>
  </body>
</html>
